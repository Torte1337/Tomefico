<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tomefico.Views.SettingsPage"
             BackgroundColor="#FDE0CD"
             xmlns:vm="clr-namespace:Tomefico.ViewModels"
             xmlns:models="clr-namespace:Tomefico.Models">

    <VerticalStackLayout Spacing="10" Margin="10" HorizontalOptions="Center" VerticalOptions="Center">
        <Label Text="Einstellungen" TextColor="Black" FontAttributes="Bold" FontSize="20" HorizontalTextAlignment="Center" VerticalTextAlignment="Center"/>
        
        <!-- <Button BackgroundColor="#FF9C9C9C" Text="Datenbank Importieren / Exportieren" IsEnabled="False"/> -->
        <Button BackgroundColor="#DA5B38" TextColor="Black" Text="Datenbank zurücksetzen" Command="{Binding ResetDatabaseCommand}"/>
        <Button BackgroundColor="#DA5B38" TextColor="Black" Text="Info" Command="{Binding GetInfosCommand}"/>
    </VerticalStackLayout>
    
</ContentPage>