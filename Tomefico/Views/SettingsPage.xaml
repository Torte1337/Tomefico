<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tomefico.Views.SettingsPage"
             BackgroundColor="#FDE0CD"
             xmlns:vm="clr-namespace:Tomefico.ViewModels"
             xmlns:models="clr-namespace:Tomefico.Models">

    <VerticalStackLayout Spacing="10" Margin="10" HorizontalOptions="Center" VerticalOptions="Center">
        <Button BackgroundColor="#DA5B38" Text="Datenbank exportieren" Command="{Binding ExportDatabaseCommand}"/>
        <Button BackgroundColor="#DA5B38" Text="Datenbank importieren" Command="{Binding ImportDatabaseCommand}"/>
        <Button BackgroundColor="#DA5B38" Text="Datenbank zurücksetzen" Command="{Binding ResetDatabaseCommand}"/>
        <Button BackgroundColor="#DA5B38" Text="Info" Command="{Binding GetInfosCommand}"/>
    </VerticalStackLayout>
    
</ContentPage>